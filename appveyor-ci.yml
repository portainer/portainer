version: 1.0.{build}
image:
  - Visual Studio 2017
  - Ubuntu
environment:
  matrix:
    - ARCH: amd64
  DOCKER_USER:
    secure: JapmC7j5F0mY3j/MVzU+Cw==
  DOCKER_PASS:
    secure: QGlCLNWzPD0HL8ipkohVic45/yU3bVOdjn0IiV6NnSQ=
  PORTAINER_VERSION: "1.19.2"
branches:
  only:
    - develop
stack: 
  - node 9, go 1.10
install:
  - yarn install
  - npm install -g rebase-docker-image
init:
  - sh: export IMAGE=linux
  - cmd: SET IMAGE=windows
  - ps: >-
      if (!(Test-Path ~/.docker)) { mkdir ~/.docker };
      Set-Content -Value '{ "experimental": "enabled" }' -Path ~/.docker/config.json -Encoding Ascii
build_script:
  - sh: yarn grunt release:$IMAGE:$ARCH
  - cmd: yarn grunt release:%IMAGE%:%ARCH%
  - sh: sudo bash build/ci-linux.sh $IMAGE $ARCH ' ' $DOCKER_USER $DOCKER_PASS ' ' $APPVEYOR_PULL_REQUEST_NUMBER
  - cmd: powershell -Command "& .\\build\\ci-windows.ps1"
notifications:
  - provider: GitHubPullRequest
    auth_token:
      secure: yC+WkZ45ZI6a4adDsjJS71L9GvbPLjTC9Q1QpTQdsqVlBb6DjEPYg6NuJ79nNFeh
    template: "{{#passed}}:white_check_mark:{{/passed}}{{#failed}}:x:{{/failed}} [Build {{&projectName}} {{buildVersion}} {{status}}]({{buildUrl}}) (commit {{commitUrl}} by @{{&commitAuthorUsername}})"
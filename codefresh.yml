version: '1.0'
steps:
  build_backend:
    image: portainer/golang-builder:ci
    working_directory: ${{main_clone}}
    commands:
      - mkdir -p /go/src/github.com/${{CF_REPO_OWNER}}
      - ln -s /codefresh/volume/${{CF_REPO_NAME}} /go/src/github.com/${{CF_REPO_OWNER}}/${{CF_REPO_NAME}}
      - ls -lah /go/src/github.com/${{CF_REPO_OWNER}}/${{CF_REPO_NAME}}
      # - /build.sh api/cmd/portainer
